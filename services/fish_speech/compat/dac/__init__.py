"""
Minimal DAC compatibility package for Fish Speech inference.

This shadows the heavyweight site-package import path so Fish can load its
custom DAC model without pulling in training-time dependencies.
"""
